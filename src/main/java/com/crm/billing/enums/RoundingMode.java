package com.crm.billing.enums;

/** Document-level total rounding rule (distinct from {@link java.math.RoundingMode}, which callers
 * must fully-qualify wherever both are needed in the same file). */
public enum RoundingMode {
    NONE, TO_AGOROT, TO_SHEKEL
}

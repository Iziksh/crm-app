package com.crm.timetracking.enums;

/** Classifies where/how PRESENCE hours were worked. Null for non-PRESENCE report types. */
public enum WorkType {
    OFFICE,
    WORK_FROM_HOME,
    FIELD_CLIENT,
    ON_CALL
}
